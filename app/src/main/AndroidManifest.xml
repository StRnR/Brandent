<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pixium.brandent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PatientDetailActivity"></activity>
        <activity android:name=".AddPatientActivity" />
        <activity android:name=".PatientsActivity" />
        <activity
            android:name=".AddFinanceActivity"
            android:parentActivityName=".ClinicActivity" />
        <activity android:name=".AddEditClinicActivity" />
        <activity
            android:name=".ClinicActivity"
            android:launchMode="singleTop" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".FinanceActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TasksActivity" />
        <activity android:name=".AddAppointmentActivity" />
    </application>

</manifest>